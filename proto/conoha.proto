syntax = "proto3";

option go_package = "pkg/grpc";

service Conoha {
    rpc ServerCommand (ServerCommandMessage) returns (ServerCommandResponse) {}
}

message ServerCommandMessage {
    string channel_id = 1;
    string bot_name = 2;
    string command = 3;
}

message ServerCommandResponse {
    string message = 1;
}
